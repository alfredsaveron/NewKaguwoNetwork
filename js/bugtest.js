
    animateLetterIn(nw, i);
  }
var words = document.getElementsByClassName('word');
var wordArray ctioction changeWord() {
  var cw = wordArray[cur currentWord = 0;

words[currentWord].style.opac
  
  for (var i = 0; i < nw.length; i++) {
    nw[i].className = 'letter behind';ds[currentWord].style.opacity = 1;
for (var i = 0; i < words.length; i++) {
  splitLetters(words[i]);
}ar nw = currentWord == words.length-1 ? wordArray[0] : wordArray[currentWord+1];
  for (var i = 0; i < cw.length; i++) {
  for (var i = 0; i < nw.length; i++) { nw[0].parentElemevar words = document.getElementsByClassName('word');
var wordArray = []; currentWord = 0co
